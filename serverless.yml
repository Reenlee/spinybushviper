service: spinybushviper

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  profile: reenlee-admin

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: /hello
          cors: true
